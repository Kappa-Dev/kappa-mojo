kappa-mojo
=============

A collection of Kappa models for inspiration and reuse.

Running models
----------------

To run all models, use ./run_models.sh. This will run KaSim on all the models, producing snapshots and output files.